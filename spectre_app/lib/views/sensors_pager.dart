import 'package:flutter/material.dart';

class SensorsPager extends StatelessWidget {
  const SensorsPager({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Sensors Pager'),
    );
  }
}
